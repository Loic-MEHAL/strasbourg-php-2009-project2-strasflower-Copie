{% block content2 %}
    <h1>Commande n° {{ command.id }}</h1>
    {{ dump(commandUser) }}

    {% for commande in commandUser %}

    <div class="modal-header">
        <h4 class="modal-title text-center" id="exampleModalLongTitle">Commande n°{{command.id}}</h4>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>

    <div class="modal-body">
        <p class="card-text">{{ dump(commandUser) }} {{commande.article}}</p>
    </div>

    {% endfor %}
{% endblock %}